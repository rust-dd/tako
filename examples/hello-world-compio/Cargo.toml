[package]
name = "hello-world"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
compio = { version = "0.18.0", features = ["macros", "net"] }
tako-rs = { path = "../../tako-rs", features = ["compio"] }

[workspace]
