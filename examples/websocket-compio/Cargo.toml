[package]
name = "websocket-compio"
version = "0.1.0"
edition = "2024"

[dependencies]
compio = { version = "0.18.0", features = ["macros", "time", "io", "ws"] }
tako-rs = { path = "../../tako-rs", features = ["compio-ws"] }
tracing = "0.1.41"

[workspace]
