[package]
name = "tls-compio"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
compio = { version = "0.18.0", features = ["macros", "net"] }
http = "1"
tako-rs = { path = "../../tako-rs", features = ["compio-tls"] }

[workspace]
